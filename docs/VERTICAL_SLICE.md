# Vertical slice

The target flow is `MANDATE → ACQUISITION → EVIDENCE → EVALUATION → DECISION → TICKET`. Phase 0 must use a real Telegraph miner and must preserve raw provenance alongside normalized evidence.

